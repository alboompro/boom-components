---
name: Dropdown
menu: Data Display
---

import { Playground, Props } from "docz"

import Dropdown from "./Dropdown"
import Button from "../../general/button"
import Icon from "../../general/icon"

# Dropdown

Um menu dropdown.

## Quando Utilizar

Quando um conteúdo ou menu precisa ser exibido em uma janela dropdown.

## Exemplos

### Uso Básico

```jsx
import Dropdown from "@alboom/boom-components/data-display/dropdown"

const MyComponent = () => {
  const dropdownContent = (
    <div>
     Fico oculto até interagirem com o botão.
    </div>
  )
  return (
    <Dropdown overlay={dropdownContent}>
      <a>Clique aqui</a>
    </Dropdown>
  )
}
```

<Playground>
  <Dropdown trigger="click" overlay={<div>Fico oculto até interagirem com o botão.</div>}>
    <Button type="primary">Clique aqui!</Button>
  </Dropdown>
</Playground>

### Alinhamento

Você pode controlar o alinhamento do dropdown através da propriedade align:

<Playground>
  <Dropdown trigger="click" align="left" overlay={<div>Alinhado a esquerda</div>}>
    <Button type="primary">Clique aqui!</Button>
  </Dropdown>
</Playground>

<Playground>
  <Dropdown trigger="click" align="center" overlay={<div>Alinhado ao centro</div>}>
    <Button type="primary">Clique aqui!</Button>
  </Dropdown>
</Playground>

<Playground>
  <Dropdown trigger="click" align="right" overlay={<div>Alinhado a direita</div>}>
    <Button type="primary">Clique aqui!</Button>
  </Dropdown>
</Playground>

### Componente controlado

<Playground state={{visible: false}}>
  {class Example extends React.Component {
    constructor(){
      super()
      this.state = {
        visible: false
      }
    }

    render(){
      return (
        <Dropdown visible={this.state.visible} onVisibleChange={(visible) => this.setState({visible: visible})} trigger="click" overlay={<div>Fico oculto até interagirem com o botão.</div>}>
          <Button type="primary">Clique aqui!</Button>
        </Dropdown>
      )
    }
  }}
</Playground>

## API

<Props of={Dropdown} />