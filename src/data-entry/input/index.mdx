---
name: Input
menu: Data Entry
---

import { Playground, Props } from 'docz'

import Input from './Input'

# Input

Basic text field to get user input.

### Basic usage

<Playground>
  <Input placeholder="default size" />
  <br />
  <Input size="large" placeholder="large size" />
</Playground>

## API

<Props of={Input} />

The rest of the props of Input are exactly the same as the original React component.
