---
name: Switch
menu: Data Entry
---

import { Playground, Props } from 'docz'

import Switch from './Switch'

# Switch
<Playground state={{ switchValue: false }}>
	{class ModalTest extends React.Component {
		constructor() {
      super()
      this.state = { 
				switchValue: false
			}
    }

		render() {
			const { switchValue } = this.state	
			return <Switch checked={switchValue} onChange={() => this.setState({ switchValue: !switchValue })} />
		}
	}}
</Playground>

## API

<Props of={Switch} />
